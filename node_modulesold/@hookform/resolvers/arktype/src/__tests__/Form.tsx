import { render, screen } from "@testing-library/react";
import user from "@testing-library/user-event";
import { type } from "arktype";
import React from "react";
import { useForm } from "react-hook-form";
import { arktypeResolver } from "..";

const schema = type({
  username: "string>1",
  password: "string>1",
});

type FormData = typeof schema.infer & { unusedProperty: string };

interface Props {
  onSubmit: (data: FormData) => void;
}

function TestComponent({ onSubmit }: Props) {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<FormData>({
    resolver: arktypeResolver(schema), // Useful to check TypeScript regressions
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} data-oid="p_ft7px">
      <input {...register("username")} data-oid="2fo2cb_" />
      {errors.username && (
        <span role="alert" data-oid="1dtlbhr">
          {errors.username.message}
        </span>
      )}

      <input {...register("password")} data-oid="2976ph." />
      {errors.password && (
        <span role="alert" data-oid="-8mddjr">
          {errors.password.message}
        </span>
      )}

      <button type="submit" data-oid="1:s8jy:">
        submit
      </button>
    </form>
  );
}

test("form's validation with arkType and TypeScript's integration", async () => {
  const handleSubmit = vi.fn();
  render(<TestComponent onSubmit={handleSubmit} data-oid="gv0mqjo" />);

  expect(screen.queryAllByRole("alert")).toHaveLength(0);

  await user.click(screen.getByText(/submit/i));

  expect(
    screen.getByText("username must be more than length 1 (was 0)"),
  ).toBeInTheDocument();
  expect(
    screen.getByText("password must be more than length 1 (was 0)"),
  ).toBeInTheDocument();
  expect(handleSubmit).not.toHaveBeenCalled();
});
