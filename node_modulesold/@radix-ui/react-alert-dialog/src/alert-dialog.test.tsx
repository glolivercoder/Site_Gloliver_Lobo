import React from "react";
import { axe } from "vitest-axe";
import type { RenderResult } from "@testing-library/react";
import { cleanup, render, fireEvent } from "@testing-library/react";
import * as AlertDialog from "./alert-dialog";
import { afterEach, describe, it, beforeEach, expect } from "vitest";

const OPEN_TEXT = "Open";
const CANCEL_TEXT = "Cancel";
const ACTION_TEXT = "Do it";
const TITLE_TEXT = "Warning";
const DESC_TEXT = "This is a warning";
const OVERLAY_TEST_ID = "test-overlay";

const DialogTest = (props: React.ComponentProps<typeof AlertDialog.Root>) => (
  <AlertDialog.Root {...props} data-oid="_rmp7gg">
    <AlertDialog.Trigger data-oid="_vqkwjk">{OPEN_TEXT}</AlertDialog.Trigger>
    <AlertDialog.Overlay data-testid={OVERLAY_TEST_ID} data-oid="6_4qv9b" />
    <AlertDialog.Content data-oid="apcjgk-">
      <AlertDialog.Title data-oid="i2vhepe">{TITLE_TEXT}</AlertDialog.Title>
      <AlertDialog.Description data-oid="hzm7e6j">
        {DESC_TEXT}
      </AlertDialog.Description>
      <AlertDialog.Cancel data-oid="0jivjt_">{CANCEL_TEXT}</AlertDialog.Cancel>
      <AlertDialog.Action data-oid="elwfx_-">{ACTION_TEXT}</AlertDialog.Action>
    </AlertDialog.Content>
  </AlertDialog.Root>
);

describe("given a default Dialog", () => {
  let rendered: RenderResult;
  let title: HTMLElement;
  let trigger: HTMLElement;
  let cancelButton: HTMLElement;

  afterEach(cleanup);

  beforeEach(() => {
    rendered = render(<DialogTest data-oid="0k:is6_" />);
    trigger = rendered.getByText(OPEN_TEXT);
  });

  it("should have no accessibility violations in default state", async () => {
    expect(await axe(rendered.container)).toHaveNoViolations();
  });

  describe("after clicking the trigger", () => {
    beforeEach(() => {
      fireEvent.click(trigger);
      title = rendered.getByText(TITLE_TEXT);
      cancelButton = rendered.getByText(CANCEL_TEXT);
    });

    it("should open the content", () => {
      expect(title).toBeVisible();
    });

    it("should have no accessibility violations when open", async () => {
      expect(await axe(rendered.container)).toHaveNoViolations();
    });

    it("should focus the cancel button", () => {
      expect(cancelButton).toHaveFocus();
    });
  });
});
