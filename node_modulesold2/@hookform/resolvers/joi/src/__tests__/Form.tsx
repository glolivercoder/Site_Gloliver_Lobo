import { render, screen } from "@testing-library/react";
import user from "@testing-library/user-event";
import * as Joi from "joi";
import React from "react";
import { useForm } from "react-hook-form";
import { joiResolver } from "..";

const schema = Joi.object({
  username: Joi.string().required(),
  password: Joi.string().required(),
});

interface FormData {
  username: string;
  password: string;
}

interface Props {
  onSubmit: (data: FormData) => void;
}

function TestComponent({ onSubmit }: Props) {
  const {
    register,
    formState: { errors },
    handleSubmit,
  } = useForm<FormData>({
    resolver: joiResolver(schema), // Useful to check TypeScript regressions
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} data-oid="eosmb1j">
      <input {...register("username")} data-oid="cq:f3kt" />
      {errors.username && (
        <span role="alert" data-oid="jnrlh14">
          {errors.username.message}
        </span>
      )}

      <input {...register("password")} data-oid="eaxg4o6" />
      {errors.password && (
        <span role="alert" data-oid="p9xk_o-">
          {errors.password.message}
        </span>
      )}

      <button type="submit" data-oid="15:8q0k">
        submit
      </button>
    </form>
  );
}

test("form's validation with Joi and TypeScript's integration", async () => {
  const handleSubmit = vi.fn();
  render(<TestComponent onSubmit={handleSubmit} data-oid="es.z0xc" />);

  expect(screen.queryAllByRole("alert")).toHaveLength(0);

  await user.click(screen.getByText(/submit/i));

  expect(
    screen.getByText(/"username" is not allowed to be empty/i),
  ).toBeInTheDocument();
  expect(
    screen.getByText(/"password" is not allowed to be empty/i),
  ).toBeInTheDocument();
  expect(handleSubmit).not.toHaveBeenCalled();
});
