import { render, screen } from "@testing-library/react";
import user from "@testing-library/user-event";
import Schema, { Type, string } from "computed-types";
import React from "react";
import { useForm } from "react-hook-form";
import { computedTypesResolver } from "..";

const schema = Schema({
  username: string.min(2).error("username field is required"),
  password: string.min(2).error("password field is required"),
  address: Schema({
    zipCode: string.min(5).max(5).error("zipCode field is required"),
  }),
});

type FormData = Type<typeof schema> & { unusedProperty: string };

interface Props {
  onSubmit: (data: FormData) => void;
}

function TestComponent({ onSubmit }: Props) {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<FormData>({
    resolver: computedTypesResolver(schema), // Useful to check TypeScript regressions
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} data-oid="amlmbt5">
      <input {...register("username")} data-oid="x_ucx-1" />
      {errors.username && (
        <span role="alert" data-oid="90lx30w">
          {errors.username.message}
        </span>
      )}

      <input {...register("password")} data-oid="9s2snfw" />
      {errors.password && (
        <span role="alert" data-oid="6ydf7b3">
          {errors.password.message}
        </span>
      )}

      <input {...register("address.zipCode")} data-oid="x6hfg:n" />
      {errors.address?.zipCode && (
        <span role="alert" data-oid="nn2keo8">
          {errors.address.zipCode.message}
        </span>
      )}

      <button type="submit" data-oid="bj:ec5:">
        submit
      </button>
    </form>
  );
}

test("form's validation with computed-types and TypeScript's integration", async () => {
  const handleSubmit = vi.fn();
  render(<TestComponent onSubmit={handleSubmit} data-oid="-akft0c" />);

  expect(screen.queryAllByRole("alert")).toHaveLength(0);

  await user.click(screen.getByText(/submit/i));

  expect(screen.getByText(/username field is required/i)).toBeInTheDocument();
  expect(screen.getByText(/password field is required/i)).toBeInTheDocument();
  expect(screen.getByText(/zipCode field is required/i)).toBeInTheDocument();
  expect(handleSubmit).not.toHaveBeenCalled();
});
