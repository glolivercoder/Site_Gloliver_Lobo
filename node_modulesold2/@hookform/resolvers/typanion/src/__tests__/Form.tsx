import { render, screen } from "@testing-library/react";
import user from "@testing-library/user-event";
import React from "react";
import { useForm } from "react-hook-form";
import * as t from "typanion";
import { typanionResolver } from "..";

const schema = t.isObject({
  username: t.applyCascade(t.isString(), [t.hasMinLength(1)]),
  password: t.applyCascade(t.isString(), [t.hasMinLength(1)]),
});

interface FormData {
  unusedProperty: string;
  username: string;
  password: string;
}

interface Props {
  onSubmit: (data: FormData) => void;
}

function TestComponent({ onSubmit }: Props) {
  const {
    register,
    formState: { errors },
    handleSubmit,
  } = useForm<FormData>({
    resolver: typanionResolver(schema), // Useful to check TypeScript regressions
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} data-oid="k92x0-t">
      <input {...register("username")} data-oid="6bu_op1" />
      {errors.username && (
        <span role="alert" data-oid="440m.8m">
          {errors.username.message}
        </span>
      )}

      <input {...register("password")} data-oid="ld5kqf3" />
      {errors.password && (
        <span role="alert" data-oid="f9eivvh">
          {errors.password.message}
        </span>
      )}

      <button type="submit" data-oid="sexz-7u">
        submit
      </button>
    </form>
  );
}

test("form's validation with Typanion and TypeScript's integration", async () => {
  const handleSubmit = vi.fn();
  render(<TestComponent onSubmit={handleSubmit} data-oid="kn5wca1" />);

  expect(screen.queryAllByRole("alert")).toHaveLength(0);

  await user.click(screen.getByText(/submit/i));

  expect(
    screen.getAllByText(
      "Expected to have a length of at least 1 elements (got 0)",
    ),
  ).toHaveLength(2);
  expect(handleSubmit).not.toHaveBeenCalled();
});
