import { render, screen } from "@testing-library/react";
import user from "@testing-library/user-event";
import { Schema } from "effect";
import React from "react";
import { useForm } from "react-hook-form";
import { effectTsResolver } from "..";

const USERNAME_REQUIRED_MESSAGE = "username field is required";
const PASSWORD_REQUIRED_MESSAGE = "password field is required";

const schema = Schema.Struct({
  username: Schema.String.pipe(
    Schema.nonEmptyString({ message: () => USERNAME_REQUIRED_MESSAGE }),
  ),
  password: Schema.String.pipe(
    Schema.nonEmptyString({ message: () => PASSWORD_REQUIRED_MESSAGE }),
  ),
});

type FormData = Schema.Schema.Type<typeof schema>;

interface Props {
  onSubmit: (data: FormData) => void;
}

function TestComponent({ onSubmit }: Props) {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm({
    resolver: effectTsResolver(schema),
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} data-oid="vd1w-k7">
      <input {...register("username")} data-oid="uc71xyf" />
      {errors.username && (
        <span role="alert" data-oid="grvrfqj">
          {errors.username.message}
        </span>
      )}

      <input {...register("password")} data-oid="bb2:m7-" />
      {errors.password && (
        <span role="alert" data-oid="i4upvi9">
          {errors.password.message}
        </span>
      )}

      <button type="submit" data-oid="9gwfzi_">
        submit
      </button>
    </form>
  );
}

test("form's validation with Zod and TypeScript's integration", async () => {
  const handleSubmit = vi.fn();
  render(<TestComponent onSubmit={handleSubmit} data-oid="zz48_fl" />);

  expect(screen.queryAllByRole("alert")).toHaveLength(0);

  await user.click(screen.getByText(/submit/i));

  expect(screen.getByText(/username field is required/i)).toBeInTheDocument();
  expect(screen.getByText(/password field is required/i)).toBeInTheDocument();
  expect(handleSubmit).not.toHaveBeenCalled();
});
